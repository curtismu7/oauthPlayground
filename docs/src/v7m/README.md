# V7M (Educational Mock) Namespace

Purpose: host all V7M mock flows, components, and wiring separate from real integrations.

- Flows live under this namespace and use services from `src/services/v7m`.
- Routes: see `routes.tsx`.
- Barrel: `index.ts` exports top-level items for consumers.



