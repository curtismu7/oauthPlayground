{
	"name": "oauth-oidc-mcp-server",
	"version": "0.1.0",
	"description": "Provider-agnostic Model Context Protocol server exposing OAuth 2.0 / OIDC flows (authorization code + PKCE, client credentials, ROPC, refresh, device, token exchange RFC 8693, PAR, CIBA, DPoP, introspection, revocation, discovery, JWKS).",
	"type": "module",
	"main": "dist/index.js",
	"scripts": {
		"build": "tsc --project tsconfig.json",
		"start": "node dist/index.js",
		"dev": "tsx src/index.ts",
		"type-check": "tsc --noEmit"
	},
	"keywords": [
		"oauth",
		"oidc",
		"mcp",
		"token-exchange",
		"pkce",
		"par",
		"ciba",
		"dpop",
		"pingone"
	],
	"author": "",
	"license": "MIT",
	"dependencies": {
		"@modelcontextprotocol/sdk": "^1.20.2",
		"axios": "^1.7.7",
		"dotenv": "^16.4.5",
		"jose": "^5.9.6",
		"zod": "^3.23.8"
	},
	"devDependencies": {
		"@types/node": "^20.12.7",
		"tsx": "^4.16.2",
		"typescript": "^5.5.4"
	}
}
