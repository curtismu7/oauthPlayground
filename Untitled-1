function addNumbers(a, b) {
    return a + b;
}

console.log(addNumbers(1, 2));

function addNumbers(a, b) {
    return a + b;
}

console.log(addNumbers(1, 2));

test("addNumbers", () => {
    expect(addNumbers(1, 2)).toBe(3);
});

test("addNumbers", () => {
    expect(addNumbers(1, 2)).toBe(3);
});

test("addNumbers", () => {
    expect(addNumbers(1, 2)).toBe(3);
}); 