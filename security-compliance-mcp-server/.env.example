# OAuth Security Compliance Checker MCP Server

# Server configuration
MCP_PORT=3000
LOG_LEVEL=info

# Optional: PingOne or other OAuth provider URLs for discovery
OAUTH_ISSUER_URL=https://auth.pingone.com/YOUR_ENVIRONMENT_ID/as
